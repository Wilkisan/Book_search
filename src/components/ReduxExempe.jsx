const SEARCH_EVENT = '...';

// logic event

const listenEvent = (event) => {
    switch (event) {
        case SEARCH_EVENT:
            // 1. {...}.search
            // 2. API call
            // 3. ....
            break;
        
        default:
            break;
    }
}